name: sample_location_comparision
description: "A new Flutter project."

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1
#flutter: version - 3.29.0
environment:
  sdk: '>=3.5.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  flutter_lints: ^3.0.0
  geolocator: 14.0.1
  get_it: ^9.0.5
  intl: ^0.19.0
  location: ^8.0.1
  provider: ^6.1.5+1
  sensors_plus: ^7.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

